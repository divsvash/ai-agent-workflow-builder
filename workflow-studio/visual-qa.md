# Visual QA Notes

The desktop execution desk was checked at 1280 × 720. The persistent dark rail, warm mineral canvas, signal-knot mark, sparse orange state language, and execution console all render with strong contrast. The run timeline is visually ahead of the summary metric cards, with the numbered rail visible in the empty state and supporting trigger and approval ledgers on the right.

The mobile execution desk was checked at 390 × 844. The sidebar collapses behind the menu button, the main execution panel stacks before supporting panels, and the run rail remains readable without horizontal overflow. Summary cards become a single column and the operator note remains legible.

The frontend was verified with `pnpm check` and `pnpm build`. No backend data was fabricated during visual QA; the visible empty state is explicitly labeled as awaiting GraphQL data.
