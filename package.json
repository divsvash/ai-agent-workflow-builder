{
  "name": "ai-agent-workflow-builder",
  "private": true,
  "version": "0.1.0",
  "description": "Workflow execution engine, action handlers, and triggers for the AI Agent Workflow Builder (Agent 2 ownership area: functions/).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.6",
    "typescript": "^5.5.4"
  }
}
